# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Instagram / Meta App
# This app uses "Instagram API with Instagram Login" (Business Login) — NOT Instagram
# Basic Display and NOT Facebook Login. Dashboard: https://developers.facebook.com/apps/
#   -> Your App -> Instagram -> API setup with Instagram business login
#
# App ID / secret: use the *Instagram app ID* and *Instagram app secret* shown on that
# Instagram product page — NOT the parent Meta app ID from Settings -> Basic.
NEXT_PUBLIC_INSTAGRAM_APP_ID=
INSTAGRAM_APP_ID=
INSTAGRAM_APP_SECRET=
# Optional: parent Meta app secret (Settings -> Basic). Only needed if webhook signature
# validation fails with 401s because Meta signs deliveries with the parent app secret.
META_APP_SECRET=
# Must exactly match one of the "OAuth redirect URIs" in Instagram -> Business login settings
# (character-for-character: scheme, host, path, no trailing slash mismatch).
NEXT_PUBLIC_INSTAGRAM_REDIRECT_URI=http://localhost:3000/api/instagram/callback
# Any random string; enter the same value in the webhook subscription form in the dashboard.
# REQUIRED for webhooks: Meta's "Verify and save" fails while this is empty.
INSTAGRAM_WEBHOOK_VERIFY_TOKEN=

# Optional: override the Whisper model used for reel transcription. Defaults to
# whisper-large-v3-turbo on Groq and whisper-1 on OpenAI. Only needed if a
# provider retires that model id. The transcription key itself is stored per
# user in the AI Agent settings, not here — a gsk_ key routes to Groq.
TRANSCRIPTION_MODEL=

# Outbound safety. true = log DMs and comment replies instead of sending them.
# `npm run dev:tunnel` defaults this to true, because a tunnelled dev server
# receives the same real webhooks production does and would otherwise reply to
# real followers twice. Never set this in production.
INSTAGRAM_DRY_RUN=

# Local development through a Cloudflare Tunnel (`npm run dev:tunnel`)
# Instagram will not send OAuth redirects or webhooks to localhost, so local dev
# against the real API needs a public HTTPS hostname in front of the dev server.
#
# Leave both blank for a quick tunnel: no Cloudflare account needed, but the
# hostname is random each run and has to be re-pasted into Meta every time.
#
# Set both for a stable hostname you register with Meta once (needs a domain on
# Cloudflare — see SETUP.md):
#   cloudflared tunnel login
#   cloudflared tunnel create insta-dev
#   cloudflared tunnel route dns insta-dev dev.yourdomain.com
CF_TUNNEL_NAME=
DEV_TUNNEL_HOSTNAME=
